# CLAUDE.md

## 迭代验证准则

每次迭代完成后，必须通过 Chrome 浏览器进行验证：

1. **如果改动能通过页面验证**（UI、交互、前端功能）：验证该功能 + 跑一遍主链路（提交回测 → 查看结果）
2. **如果改动无法通过页面验证**（后端逻辑、引擎、工程改动）：统一验证一遍主链路（提交回测 → 查看结果）

主链路定义：首页选资产/模板 → 提交回测 → 自动跳转详情页 → 确认指标和图表正常显示
